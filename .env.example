# Port the server listens on. Render sets this automatically; only needed
# locally if you want something other than the default.
PORT=3000

# Hostname to allow in the Host header once deployed (DNS-rebinding
# protection). Example: order-ops-mcp.onrender.com
# Leave unset for local development.
PUBLIC_HOSTNAME=
